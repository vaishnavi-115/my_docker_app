name: Build and Deploy to Azure Container App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write       # Required for OIDC login
      contents: read       # Required to checkout repo

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Verify files (optional)
      - name: List repo files
        run: ls -R

      # 3️⃣ Login to Azure using service principal
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS_JSON }}

      # 4️⃣ Login to Azure Container Registry
      - name: Login to ACR
        run: az acr login --name vaishnaviacr

      # 5️⃣ Build and Push Docker Image
      - name: Build and Push Docker Image
        run: |
          IMAGE=vaishnaviacr.azurecr.io/myfirstcontainerapp:${{ github.sha }}
          echo "Building image $IMAGE"
          docker build -t $IMAGE .           # <-- changed to root directory
          docker push $IMAGE
          echo "IMAGE_NAME=$IMAGE" >> $GITHUB_ENV

      # 6️⃣ Deploy to Azure Container App
      - name: Deploy to Azure Container App
        run: |
          az containerapp update \
            --name myfirstcontainerapp \
            --resource-group awesome_azure_ninja \
            --image $IMAGE_NAME \
            --ingress external \
            --target-port 8000
